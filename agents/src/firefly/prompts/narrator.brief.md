## Mission intent

**From** {{ req.from_ }} → **To** {{ req.to }} — window **{{ req.window }}**

## Orbit
{% set o = c.orbit.orbit if c.orbit.orbit is defined else {} %}
| Param | Value |
| --- | --- |
| Type | {{ o.type | default("—") }} |
| Altitude | {{ o.altitude_km | default("—") }} km |
| Inclination | {{ o.inclination_deg | default("—") }}° |
| Period | {{ o.period_min | default("—") }} min |
| Eclipse fraction | {{ o.eclipse_fraction | default("—") }} |

## Radiation envelope
{% set d = c.orbit.dose_estimate if c.orbit.dose_estimate is defined else {} %}
- **1-yr TID behind 2 mm Al**: {{ d.tid_krad_per_year_behind_2mm_al | default("—") }} krad
- **SEE LET threshold (target)**: {{ d.see_let_threshold_mev_cm2_mg | default("—") }} MeV·cm²/mg
- **SEU rate**: {{ d.see_seu_rate_per_device_day | default("—") }} /device/day
- Source: {{ d.source | default("[[concepts/orbit-dose-budgeting]]") }}

## Space weather
{% set sw = c.orbit.space_weather if c.orbit.space_weather is defined else {} %}
- Kp: {{ sw.kp_index | default("—") }}  |  Solar wind: {{ sw.solar_wind_km_s | default("—") }} km/s

## Launch opportunities
{% if c.launch_opportunities.candidate_windows %}
{% for w in c.launch_opportunities.candidate_windows %}
- {{ w.provider }} @ {{ w.site }} — earliest: {{ w.earliest | default("TBD") }}
{% endfor %}
{% else %}
- *(LaunchPlannerAgent is a v2.1 stub — see `mvp_stub: true` in JSON)*
{% endif %}

## Status of the five agents
| Agent | Status |
| --- | --- |
| OrbitDesignerAgent | {{ "stub (Day 1)" if c.orbit.mvp_stub else "real" }} |
| LaunchPlannerAgent | stub (v2.1) |
| PowerThermalAgent | stub (v2.2) |
| RiskAgent | stub (v2.3) |
| NarratorAgent | thin Jinja (this brief) — v2.4 → Claude PDF |

---

*Generated by Unified Firefly — see `agents/README.md`.*
