---
slug: JAMintro
title: "JAM — The Scalable Trustless Virtual Machine"
authors: [polkasharks]
tags: [polkadot]
description: "A deep-dive into Polkadot JAM (Join-Accumulate Machine): targeting 850 MB/s throughput and 3.4M TPS via an async multi-core architecture — and how it stacks up against Ethereum and Solana."
image: /img/og/JAMintro.png
---
# What is Polkadot JAM?

Polkadot JAM (Join-Accumulate Machine) is a proposed upgrade to the Polkadot network that would replace the current Relay Chain with a more modular and minimal design. It combines Ethereum's smart-contract capabilities with Polkadot's scalability to create a more flexible development environment.

## Key Features

- **Permissionless:** Developers can deploy code for a resource-usage fee, similar to Ethereum smart contracts.
- **Coretime:** Uses "coretime" as the unit of compute resource — analogous to Ethereum's gas.
- **Service architecture:** Runs various services (e.g., ChainService for processing parachains), making the network more flexible.
- **Async interactions:** Unlike many blockchain platforms, JAM interactions are asynchronous, improving throughput.
- **High throughput:** Targets 850 MB/s via multi-core computation — exceeding Ethereum and Solana.

## Architecture and Technical Foundation

JAM's compute model is based on the "Join-Accumulate" mechanism. The **Join** and **Accumulate** functions execute on-chain, while **Collect** and **Refine** run off-chain. This offloads work from the chain and increases efficiency. Per the Polkadot Wiki, JAM is a domain-specific chain focused on processing roll-ups with strong security guarantees.

JAM also supports asynchronous interactions — a departure from the synchronous model of most smart-contract platforms. Messages and token transfers complete within the same 6-second execution epoch, but without an instant return path, opening the door to future performance optimizations like dynamic gas allocation.

## Performance Targets

JAM's performance targets are ambitious. According to published specs, JAM plans to use **350 cores**, each with a 6-second execution window and 5 MB input — yielding **850 MB/s total throughput** and theoretically supporting **over 3.4 million TPS**. For comparison:

- Ethereum 2.0: **1.3 MB/s**
- Sui / Aptos: **100 MB/s**
- Solana (post-Firedancer): **1,250 MB/s**

To achieve this, JAM will use a **RISC-V processor architecture**, replacing the current WebAssembly-based framework. RISC-V is an open-source, widely-adopted ISA already used by Google, NVIDIA, and Alibaba — chosen to improve computational efficiency and enable multi-core parallel execution.

**Testing infrastructure:**

- **Polkadot Palace:** A supercomputer in Lisbon, Portugal with **12,276 cores and 16 TB RAM** — capable of hosting a full JAM network.
- **JAM Toaster simulator:** Supports **1,023 nodes**, equipped with **16,384 AMD CPUs, 32 TB RAM, and 20 PB storage**.

## Economics and Governance

JAM's economic model centers on the **DOT token**. Developers purchase "coretime" to run computations — analogous to buying gas on Ethereum.

JAM will also introduce **Agile Coretime**, a more economical flexible model allowing users to bulk-purchase coretime by the month and split or resell it on secondary markets. All transactions use **DOT**. This market mechanism improves capital efficiency of compute resources.

On governance: JAM preserves Polkadot's decentralized leadership model — upgrades require approval by DOT-holder vote. On May 28, 2024, the JAM upgrade passed with near-unanimous support from over **31 million DOT**, signaling strong community endorsement.

## Current Status

JAM is still in development, estimated to take approximately **2 years** to be fully deployment-ready. The upgrade will ship as a single transition rather than a series of incremental steps, to avoid the complexity of multiple small-scale updates. Existing **parachain** functionality will not be disrupted, and DOT holders and traders will not face interruption during the upgrade.

To accelerate development, the **Web3 Foundation has set aside a 10 million DOT "JAM Implementer's Prize"** to encourage multiple independent client implementations — strengthening network resilience.

## Developer Impact

For developers, JAM removes the need to compete in parachain slot auctions. A service is deployed by specifying just three entry functions: **Refine**, **Accumulate**, and **onTransfer**. This simplifies the development pipeline and makes it easier for Polkadot to support **smart contracts, ZK-rollups, UTXO models**, and other paradigms.

## Security and Scalability

JAM's security mechanism includes **SAFROLE (SNARK-based block production)** — a zero-knowledge-proof-based block production method that raises the security bar and reduces the need for disruptive updates.

JAM's **multi-core processing** significantly expands Polkadot's compute capacity, enabling developers to deploy applications at lower cost.

## Positioning vs. Ethereum and Solana

Compared to Ethereum and Solana, JAM's high throughput and multi-core architecture give it a significant performance advantage. Its **850 MB/s data-availability target** is **42×** the current Polkadot capacity (pre-Async Backing baseline). This performance leap positions Polkadot favorably in the ongoing blockchain scalability race.

## Conclusion

Polkadot JAM is an ambitious upgrade proposed by **Gavin Wood** to transform Polkadot into a **more flexible, efficient, and scalable** blockchain platform. By combining Ethereum's smart-contract capabilities with Polkadot's multi-chain architecture, JAM gives developers a larger innovation surface while preserving network security and decentralization.

Though full deployment is still years away, the potential has been widely recognized by the community — and approved by vote.
